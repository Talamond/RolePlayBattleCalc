export default class Stat {

	constructor(score, modifer, tempModifer, die) {
		this.props.score = score;
		this.props.modifer = modifer;
		this.props.tempModifer = tempModifer;
		this.props.die = die;
	}
}
